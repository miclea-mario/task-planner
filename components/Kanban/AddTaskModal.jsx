import { Modal } from '@components';

const AddTaskModal = ({ isOpen, hide }) => {
  return (
    <Modal isOpen={isOpen} hide={hide} title="Add new task">
      <h1>aa</h1>
    </Modal>
  );
};

export default AddTaskModal;
